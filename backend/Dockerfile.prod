FROM python:3.11-slim

WORKDIR /app

# Build context is monorepo root; copy backend contents
COPY backend/ .
RUN pip install --no-cache-dir .

EXPOSE 8080
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8080"]
